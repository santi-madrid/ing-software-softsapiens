name: Test CharacterView Movement

on:
  pull_request:
    branches:
      - develop  # 🔹 Solo ejecuta los tests cuando se mergea a develop
    types:
      - closed  # 🔹 Solo cuando el PR se cierra (mergeado)

jobs:
  test_character_view_movement:
    runs-on: ubuntu-latest  # 🔹 Usamos un entorno Linux en GitHub Actions

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3  # 🔹 Descarga el código del repositorio

    - name: Install dependencies
      run: sudo apt-get install libgtest-dev cmake make g++  # 🔹 Instala Google Test

    - name: Build tests
      run: |
        mkdir build
        cd build
        cmake ..
        make  # 🔹 Compila los tests

    - name: Run CharacterView movement tests
      run: ./build/test_character_view  # 🔹 Ejecuta el test
